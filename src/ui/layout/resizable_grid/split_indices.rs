/// Root split index between the left-side pane group and the main pane.
pub const SHELL_SPLIT_INDEX: usize = 0;

/// Nested split index between workspace and session panes.
pub const WORKSPACE_SPLIT_INDEX: usize = 1;
